Pull the latest changes from the remote origin main branch into the current local branch.

Steps:
- Run git pull origin main.
- Report what changed (files updated, already up to date, or any conflicts).
- If there are merge conflicts, list the conflicting files and ask the user how to resolve them.
