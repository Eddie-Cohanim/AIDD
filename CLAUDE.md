# AIDD Workshop — Claude Instructions

## Project Overview
AI-Driven Development (AIDD) workshop project.

## Environment Setup
- Copy `.env` to `.env.local` and fill in API keys
- Required: `ANTHROPIC_API_KEY`

## Custom Skills
Custom slash commands live in `.claude/commands/`. Each `.md` file is a `/skill-name` command.

## Guidelines
- Keep code simple and focused
- Document new skills in `.claude/commands/`
